import 'package:flutter/material.dart';

class AppColors {
  // ── Al Tanfeethi Brand Palette ───────────────────────────────────────────
  // Extracted from altanfeethi.com: copper/rose-gold accent on deep charcoal

  /// Copper / rose-gold — primary brand accent (buttons, highlights, icons)
  static const Color primary = Color(0xFFC4845A);

  /// Lighter copper tint — hover states, active indicators
  static const Color primaryLight = Color(0xFFD4A882);

  /// Deep charcoal navy — main dark background
  static const Color darkBlue = Color(0xFF1A1A2E);

  /// Slightly lighter charcoal — card backgrounds, secondary surfaces
  static const Color cardBackground = Color(0xFF2C2C3E);

  /// App screen background — off-white warm
  static const Color background = Color(0xFFF8F4F0);

  /// Field border — warm light grey
  static const Color fieldBorder = Color(0xFFDDD5CC);

  /// Subtitle / hint text — muted warm grey
  static const Color subtitleText = Color(0xFF8A7D74);

  /// Bottom bar background
  static const Color bottomBar = Color(0xFF1A1A2E);

  /// Divider color
  static const Color divider = Color(0xFFE8E0D8);
}